# Firebase Emulator Mode
# Use this for local development without burning API quotas

VITE_USE_FIREBASE_EMULATOR=true

# Backend URL (must match your running backend)
VITE_API_URL=http://localhost:3000

# No need to set Firebase config - emulator uses demo values
